name: Build DilV Windows Release

on:
  workflow_dispatch:  # Manual trigger
  push:
    tags:
      - 'v*'  # Trigger on version tags (same tags as DIL releases)

jobs:
  build-dilv-windows:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Set up MSYS2
      uses: msys2/setup-msys2@v2
      with:
        update: true
        install: >-
          mingw-w64-x86_64-gcc
          mingw-w64-x86_64-make
          mingw-w64-x86_64-leveldb
          mingw-w64-x86_64-openssl
          mingw-w64-x86_64-cmake
          mingw-w64-x86_64-miniupnpc
          mingw-w64-x86_64-gmp
          git
          make

    - name: Build RandomX (for shared Makefile compatibility)
      shell: msys2 {0}
      run: |
        cd depends/randomx
        mkdir -p build-windows
        cd build-windows
        cmake -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Release ..
        mingw32-make -j4

    - name: Build dilv-node
      shell: msys2 {0}
      run: |
        mingw32-make dilv-node check-wallet-balance -j4

    - name: Extract version from tag
      id: get_version
      shell: bash
      run: |
        if [[ $GITHUB_REF == refs/tags/* ]]; then
          VERSION=${GITHUB_REF#refs/tags/}
        else
          VERSION="v1.0.0"
        fi
        echo "version=$VERSION" >> $GITHUB_OUTPUT
        echo "Detected version: $VERSION"

    - name: Package Windows Release
      shell: msys2 {0}
      env:
        VERSION: ${{ steps.get_version.outputs.version }}
      run: |
        chmod +x package-dilv-windows-release-github.sh
        ./package-dilv-windows-release-github.sh

    - name: Upload artifact
      uses: actions/upload-artifact@v7
      with:
        name: dilv-${{ steps.get_version.outputs.version }}-mainnet-windows-x64
        path: releases/dilv-${{ steps.get_version.outputs.version }}-mainnet-windows-x64.zip

    - name: Upload to release (if tag)
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: releases/dilv-${{ steps.get_version.outputs.version }}-mainnet-windows-x64.zip
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
